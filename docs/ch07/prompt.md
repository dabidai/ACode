给ACode装上上下文管理能力，保证Agent能在有限的Token预算内长时间工作，不会因为对话太长而瘫掉。

具体要新增：

Token预算：近似计算当前对话占了多少Token

大结果存磁盘：工具输出超过阈值时，完整内容写到本地文件，对话里只保留预览和路径

全量摘要(auto-compact)：上下文逼近窗口上限时，调用LLM生成结构化摘要替换旧消息，压缩后恢复关键上下文

手动/compact命令：用户随时可以触发压缩，显示压缩后的Token变化

**不做**：精确tokenizer(近似估计就够了)、压缩策略的机器学习优化