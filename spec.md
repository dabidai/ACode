# ACode 执行边界与异步生命周期修复规格

## 背景

ACode 会执行本地命令、远端 MCP 工具，并在后台提取记忆。当前部分权限判断、断线重试和取消流程无法保证用户预期的执行边界，可能导致未经确认的命令、重复远端操作或旧会话结果迟到写入。

## 目标用户

- 使用 ACode 执行本地与 MCP 工具的开发者。
- 在长任务中取消、清空或切换对话的用户。

## 能力清单

- 安全命令自动放行只适用于明确识别的单条只读命令。
- 不能明确识别为安全的命令进入正常权限规则和确认流程。
- MCP 调用发生结果不确定的连接故障时，不自动重复执行写操作。
- 记忆提取在会话重置后不写入属于旧会话的结果。
- 用户取消一轮执行后，旧工具结束才允许后续一轮开始。
- 失败与取消状态向用户或模型提供可理解的结果。

## 非功能要求

- 保留现有工具协议与会话文件兼容性。
- 权限判断对 Windows 与类 Unix shell 都采取保守边界。
- 不修改数据库。
- 四项修复分别完成回归验证、Git 提交与 GitHub 推送。

## 设计骨架

- 命令许可逻辑先判定是否为单条、可识别的只读命令，再进入权限决策。
- MCP 连接恢复与工具调用重试分开处理；写调用发送后发生故障则返回结果未知。
- 记忆写入与会话代次重置共用同步边界。
- 工具执行任务的取消与下一轮启动由同一个生命周期边界协调。

## Out of Scope

- 对任意 shell 脚本做完整语法解析。
- 为所有外部 MCP 服务提供跨服务的幂等事务保证。
- 修改记忆内容格式、数据库或终端布局。
