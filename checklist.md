# ACode 执行边界与异步生命周期验收清单

## 权限

- [x] 输入含换行的 `ls` 加第二条命令时，不返回安全命令自动放行。
- [x] 输入含单个 `&`、管道、重定向或命令替换时，不返回安全命令自动放行。
- [x] 单条 `git status` 仍可按安全命令自动放行。
- [x] 未识别命令继续经过权限模式和确认流程。

## MCP

- [x] 调用前发现连接已断时，可先恢复连接并只发送一次工具调用。
- [x] 写工具请求已发送后连接中断时，连接建立次数仍为 1，调用不重发。
- [x] 上述结果未知场景返回失败并明确提示可能已执行。
- [x] 远端明确返回 `isError` 的行为保持失败结果。

## 记忆

- [x] 后台提取正在等待模型回复时执行 `/clear`，旧结果写入数为 0。
- [x] 重置与已开始的写回互斥；重置返回后，旧任务写入数不再增加。
- [x] 重置后新一轮提取仍能写入新会话结果。

## 取消

- [x] 取消正在执行的长工具后，下一轮启动前旧工具已结束。
- [x] 取消后旧工具结果不进入新一轮历史。
- [x] 并发读工具与串行写工具都遵守取消边界。
- [x] 用户能看到取消或等待收尾的明确状态。

## 集成与交付

- [x] `McpEndToEndTest`、`MemorySystemEndToEndTest` 与控制器取消测试在全量回归中通过。
- [x] 全量测试在可写环境下通过：1138 tests、0 failures、0 errors、1 skipped。
- [x] 四项风险各有独立 Git 提交，且均已推送到 `origin`。
- [x] 数据库及其存储文件没有改动。
