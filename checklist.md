# ACode 执行边界与异步生命周期验收清单

## 权限

- [x] 输入含换行的 `ls` 加第二条命令时，不返回安全命令自动放行。
- [x] 输入含单个 `&`、管道、重定向或命令替换时，不返回安全命令自动放行。
- [x] 单条 `git status` 仍可按安全命令自动放行。
- [x] 未识别命令继续经过权限模式和确认流程。

## MCP

- [ ] 调用前发现连接已断时，可先恢复连接并只发送一次工具调用。
- [ ] 写工具请求已发送后连接中断时，测试服务端记录的调用次数为 1。
- [ ] 上述结果未知场景返回失败并明确提示可能已执行。
- [ ] 远端明确返回 `isError` 的行为保持失败结果。

## 记忆

- [ ] 后台提取正在等待模型回复时执行 `/clear`，旧结果写入数为 0。
- [ ] 在代次校验与写回之间触发重置，旧结果写入数为 0。
- [ ] 重置后新一轮提取仍能写入新会话结果。

## 取消

- [ ] 取消正在执行的长工具后，下一轮启动前旧工具已结束。
- [ ] 取消后旧工具结果不进入新一轮历史。
- [ ] 并发读工具与串行写工具都遵守取消边界。
- [ ] 用户能看到取消或等待收尾的明确状态。

## 集成与交付

- [ ] 从真实会话入口运行一次“命令确认 → MCP 断线 → 取消长工具 → 清空会话”组合流程，四处行为符合以上断言。
- [ ] 全量测试在可写环境下通过；若仍有失败，逐项记录原因。
- [ ] 四项风险各有独立 Git 提交，且均已推送到 `origin`。
- [ ] 数据库及其存储文件没有改动。
