# 终端缩放稳定性修复规格

## 背景

ACode 在等待用户输入时会显示模式信息、输入区和固定页脚。真实 Windows CMD 中，先前版本虽然保留了输入内容，窗口缩放后仍堆叠模式行和长分隔线；需要修复 Windows 终端回流造成的残影和错位。

## 目标用户

- 在原生 CMD、PowerShell、Windows Terminal 中交互使用 ACode 的用户。
- 会在等待输入或编辑多行内容时调整终端窗口大小的用户。

## 能力清单

- 等待输入期间缩放窗口不会产生重复页眉、重复分隔线或控制序列文本。
- 缩放过程中已经输入的文本保持完整，并且只能提交一次。
- 输入框从上到下显示模式行、上分隔线、输入标记、下分隔线、模型状态行。
- 在实时画面中输入框固定在终端底部；长输入或多行输入使输入区向上增高，边线和模型状态行仍保留。
- 输入区和页脚最终使用新的终端宽度排版，页脚不触发最右列自动折行。
- 缩放后下一轮输入恢复到稳定位置，不继承上一轮的旧尺寸偏移。
- 连续快速缩放只保留一份活动输入界面。
- 缩放和提交后输入框继续贴底，不覆盖上方对话内容。
- 运行时可继续使用终端原生滚轮查看 ACode 对话历史。
- 原生 CMD 继续支持直接用鼠标拖选和复制对话文字。
- 不支持活跃区能力的终端继续使用现有降级行为。

## 非功能要求

- 活动输入期间不得通过终端输入流查询光标位置。
- 一次尺寸变化只能有一个输入区重绘所有者。
- 不依赖第三方终端库的私有字段或反射访问。
- 缩放处理不得阻塞用户输入，不得创建持续轮询线程。
- 保持既有命令、模型调用、MCP、会话和权限行为不变。
- 全量自动化测试继续通过，并完成真实 Windows 终端验收。

## 设计骨架

- 输入框的模式行、两条边线、编辑内容和模型信息由同一个底部绘制区域管理。
- 窗口缩放后重建当前对话视图和活动输入框，同时保持已提交的对话可在终端滚轮历史中查看。
- 长输入让编辑区域向上扩展，底部模型信息始终保留。
- 自动化重点验证输入内容和尺寸变化的不变量；真实终端测试验证操作系统回流后的最终视觉结果。

## Out of Scope

- 保留启动 ACode 前的终端历史内容。
- 在终端原生滚轮回看的旧视口上继续固定显示活动输入框；回到最新画面后输入框恢复贴底。
- 保证所有终端模拟器和字体下像素级一致。
- 为不具备活跃区能力的终端实现同样的固定底部界面。
- 修改模型调用、MCP 协议、数据库或会话存储格式。
- 本次修复之外的阶段十及后续功能。
